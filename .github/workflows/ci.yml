name: ci
on: push
jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3

      - name: Run tests 🧪
        uses: cypress-io/github-action@v2
        env:
          DEBUG: color-picker
